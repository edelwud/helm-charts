apiVersion: v1
kind: Service
metadata:
  name: {{ include "vm-proxy-auth.fullname" . }}
  labels:
    {{- include "vm-proxy-auth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vm-proxy-auth.selectorLabels" . | nindent 4 }}
